<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Base: tono azul de la paleta para tarjetas -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="20dp"/>
            <gradient
                android:type="linear"
                android:angle="90"
                android:startColor="@color/azul_bosque"
                android:centerColor="@color/azul_neblina"
                android:endColor="@color/azul_medianoche"/>
            <!-- stroke con tono neblina ligeramente opaco -->
            <stroke android:width="1dp" android:color="@color/azul_neblina"/>
        </shape>
    </item>

    <!-- Brillo superior muy suave en blanco lunar con baja opacidad -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="20dp"/>
            <gradient
                android:type="linear"
                android:angle="90"
                android:startColor="#14E6EEF8"
                android:endColor="#00000000"/>
        </shape>
    </item>

    <!-- Vignette interno para profundidad usando overlay oscuro sutil -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="20dp"/>
            <gradient
                android:type="radial"
                android:centerX="50%"
                android:centerY="50%"
                android:gradientRadius="420dp"
                android:startColor="#00000000"
                android:endColor="#22000000"/>
        </shape>
    </item>

</layer-list>
